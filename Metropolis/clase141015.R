

#devuelve un vector
recibe la distribucion 
theta 0
numero de iteraciones

recupera tamaño vector distr.size

X
U

vector thetas(niter)


for(i a niter)
  U = runif(0,1)
X = floor(M*(R::unif(0,1)))
if(U<= distr[X]/distr[thetas[i-1]])
  thetas[]